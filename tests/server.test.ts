import request from 'supertest';
import app from '../server';

describe('Test API endpoints', () => {
    test('should return welcome message on / GET', () => {
        
    });

    // test('welcome', async () => {
    //     const server = await request(app).get('/')
    //     expect(server.body).toContain({
    //         message: "Welcome"
    //     })
    // })

});

